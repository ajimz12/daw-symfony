{% extends 'base.html.twig' %}

{% block title %}ChatGroup index{% endblock %}

{% block body %}
    <h1>ChatGroup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for chat_group in chat_groups %}
            <tr>
                <td>{{ chat_group.id }}</td>
                <td>{{ chat_group.name }}</td>
                <td>
                    <a href="{{ path('app_chat_group_show', {'id': chat_group.id}) }}">show</a>
                    <a href="{{ path('app_chat_group_edit', {'id': chat_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_chat_group_new') }}">Create new</a>
{% endblock %}
